< Minimal Spawn Test - Debug >

blueprint Box {
    shard x
}

spec test_spawn() {
    show("Before spawn")
    firm b = spawn Box()
    show("After spawn")
    show("Success!")
}

funcall test_spawn()
