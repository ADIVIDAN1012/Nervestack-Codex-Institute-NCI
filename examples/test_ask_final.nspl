spec test1() {
    firm num = convert 42 to Num
    show("Direct number: " + convert num to Text)
}

spec test2() {
    show("Testing ask() with input '42':")
    firm x = ask("")
    show("Received value (as text): " + convert x to Text)
}

funcall test1()
funcall test2()
