< This is a comment >
x = On
y = Off
z = Nil
check(x == On) {
    show("x is On")
}
nick MyInt = Num
a = convert 10 to Text
check(10 > 5) { show("Greater") }
check(5 <10) { show("Less") }
check(5 <= 5) { show("Less Equal") }
check(10 >= 10) { show("Greater Equal") }
< Comment with space >
<CommentWithoutSpace>
check(5 < 10) { show("Less with space") }
check(10 '= 5) { show("Not Equal using '=") }
