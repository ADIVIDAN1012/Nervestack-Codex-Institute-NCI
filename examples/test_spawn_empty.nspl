< Test Spawn with Empty Parentheses >

blueprint SimpleBox {
    shard value
}

spec main() {
    show("Creating box with spawn...")
    firm box = spawn SimpleBox()
    show("Box created!")
    box.value = 42
    show("Value set!")
    show(box.value)
}

funcall main()
